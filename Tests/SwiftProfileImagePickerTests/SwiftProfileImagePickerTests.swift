import XCTest
@testable import SwiftProfileImagePicker

final class SwiftProfileImagePickerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(SwiftProfileImagePicker().text, "Hello, World!")
    }
}
